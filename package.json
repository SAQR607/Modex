{
  "name": "modex-platform",
  "version": "1.0.0",
  "description": "MODEX Competition Platform - Full Stack Application",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend && npm run sync:frontend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "sync:frontend": "node scripts/sync-frontend.js",
    "deploy": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}


